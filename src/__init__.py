from .parameter import Param # parameter.py 의 Param 클래스만 가져옴

from . import gradients # gradients 폴더의 모든 모듈을 가져옴 
from . import layers # layer 폴더의 모든 모듈을 가져옴 
from . import optimizers # optimizers 폴더의 모든 모듈을 가져옴
from .ops.manipulate import *